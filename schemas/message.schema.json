{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://carqo.org/schemas/message.schema.json",
  "title": "Message",
  "type": "object",
  "required": ["id", "actors", "shipments", "createdAt", "updatedAt"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier of this message."
    },
    "shipments": {
      "type": "array",
      "description": "List of shipments in this message. Must contain at least one item.",
      "items": { "$ref": "shipment.schema.json" },
      "minItems": 1
    },
    "actors": {
      "type": "array",
      "description": "List of actrors in this message. Must contain at least one item.",
      "items": { "$ref": "actor.schema.json" },
      "minItems": 1
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Datetime of this message's creation in ISO 8601 format."
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Datetime of this message's last update in ISO 8601 format."
    }
  },
  "additionalProperties": true
}
